[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gigaevo"
version = "1.28.0"
description = "Advanced evolutionary computation framework with MAP-Elites and DAG-based program execution"
authors = [
    {name = "GigaEvo Team", email = "team@gigaevo.ai"}
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.12"

dependencies = [
    "redis>=4.5.0",
    "loguru>=0.7.0",
    "numpy>=2.0.0",
    "pydantic>=2.0.0",
    "psutil>=5.9.0",
    "fakeredis[lua]>=2.19.0",
    "orjson",
    "diffpatch",
    "openai[aiohttp]>=2.0.0",
    "aiohttp>=3.10",
    "requests>=2.31",
    "PyYAML>=6.0",
    "chromadb>=0.4.22",
    "sentence-transformers>=2.2.2",
    "nltk>=3.8.1",
    "transformers>=4.36.2",
    "litellm>=1.16.11",
    "scikit-learn>=1.3.2",
    "tqdm>=4.65.0",
    "networkx",
    "scipy",
    "pandas",
    "wandb",
    "cloudpickle",
    "loky>=3.5",
    "matplotlib",
    "pandas==2.2.2",
    "seaborn",
    "statsmodels",
    "tensorboardX",
    "python-dotenv",
    "click",
    "tyro>=0.8",
    "jinja2",
    "langgraph>=0.2.0",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.2.0",
    "langchain>=0.3.0",
    "langfuse>=2.0.0",
    "scikit-learn",
    "optuna",
    "cma",
    "bm25s>=0.3.0",
    "PyStemmer>=3.0.0",
    "gigaevo-memory",
]

[project.optional-dependencies]
dev = ["pre-commit", "python-semantic-release"]
typing = ["mypy>=1.10", "types-PyYAML", "types-redis", "types-psutil"]
# Per-experiment extras: install with ``uv pip install -e '.[<name>]'``.
# Each set covers the third-party deps a problem family imports outside
# the core [project] dependencies block.
alphaevolve = [
    "jax",
    "optax",
    "sympy>=1.13",
]
chains = [
    "absl-py",
    "datasets>=2.14",
    "emoji",
    "immutabledict",
    "langdetect",
    "packaging>=23",
    "syllapy",
    "tenacity>=8.2",
]
geometry = [
    "shapely>=2.0",
]
prompts = [
    "absl-py",
    "datasets>=2.14",
    "emoji",
    "immutabledict",
    "langdetect",
    "packaging>=23",
    "syllapy",
    "tenacity>=8.2",
]
all = [
    "absl-py",
    "datasets>=2.14",
    "emoji",
    "immutabledict",
    "jax",
    "langdetect",
    "optax",
    "packaging>=23",
    "shapely>=2.0",
    "syllapy",
    "sympy>=1.13",
    "tenacity>=8.2",
]
# Aliases retained for backward compatibility with earlier extra names.
opt = ["jax", "optax"]
prompt = ["emoji", "immutabledict", "langdetect", "nltk", "syllapy"]
test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "pytest-timeout>=2.4",
    "pytest-xdist>=3.6",
    "fakeredis[lua]>=2.19.0",
]

[project.urls]
Repository = "https://github.com/gigaevo/gigaevo"
Documentation = "https://gigaevo.readthedocs.io"
Issues = "https://github.com/gigaevo/gigaevo/issues"
Changelog = "https://github.com/gigaevo/gigaevo/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
include = ["gigaevo*"]

[tool.ruff]
line-length = 88  # same as in black
target-version = "py312"
exclude = [".claude/worktrees", "experiments/*/top_programs_*", "gigaevo/memory/_vendor/A_mem/", "gigaevo/memory/_vendor/GAM_root/"]

[tool.ruff.lint]
# pycodestyle (E), Pyflakes (F), isort (I), pyupgrade (UP),
# flake8-tidy-imports (TID) for the banned-api policy below.
select = ["E", "F", "I", "UP", "TID"]
ignore = ["E501"]  # Ignore line-too-long (handled by ruff-format)

# Banned-import policy: redis imports must route through
# ``gigaevo.dataplane``; pickle deserialization is banned across trust
# boundaries. Local subprocess-IPC exemptions live in per-file-ignores.
[tool.ruff.lint.flake8-tidy-imports.banned-api]
"redis".msg = "Import from gigaevo.dataplane instead — see gigaevo/dataplane/__init__.py"
"redis.asyncio".msg = "Import from gigaevo.dataplane instead — see gigaevo/dataplane/__init__.py"
"redis.exceptions".msg = "Import from gigaevo.dataplane instead — see gigaevo/dataplane/__init__.py"
"pickle.loads".msg = "Pickle deserialization is unsafe across trust boundaries; use a typed wire format or restrict to the per-file-ignores allowlist."
"cloudpickle.loads".msg = "Pickle deserialization is unsafe across trust boundaries; use a typed wire format or restrict to the per-file-ignores allowlist."

[tool.ruff.lint.per-file-ignores]
"run.py" = ["E402"]
"tools/**/*.py" = ["E402", "TID251"]
"__init__.py" = ["F401"]
"problems/**/*.py" = ["E402", "E722", "E741", "F841", "TID251"]
"gigaevo/memory/ideas_tracker/run_ideas_tracker_from_csv.py" = ["E402"]
"gigaevo/memory/examples/a_mem_memory_creation.py" = ["E402"]
"gigaevo/memory/shared_memory/amem_gam_retriever.py" = ["E402"]
"gigaevo/memory/shared_memory/memory.py" = ["E402"]
"gigaevo/memory/examples/memory_usage_example.py" = ["E402"]
"gigaevo/memory_platform/shared_memory/memory.py" = ["E402"]
"gigaevo/memory_platform/shared_memory/remote_gam_retriever.py" = ["E402"]
# Dataplane owns the Redis boundary.
"gigaevo/dataplane/**/*.py" = ["TID251"]
# Reference Lua-CAS implementation that predates the dataplane.
"gigaevo/infra/endpoint_pool.py" = ["TID251"]
# Redis call sites not yet routed through the dataplane.
"gigaevo/database/redis/**/*.py" = ["TID251"]
"gigaevo/database/redis_program_storage.py" = ["TID251"]
"gigaevo/evolution/bus/transport.py" = ["TID251"]
"gigaevo/evolution/storage/archive_storage.py" = ["TID251"]
"gigaevo/utils/trackers/backends/redis.py" = ["TID251"]
# Subprocess-IPC sites: cloudpickle.loads consumes a payload produced
# by a child process this module itself spawned; the trust boundary is
# local.
"gigaevo/programs/stages/python_executors/wrapper.py" = ["TID251"]
"gigaevo/programs/stages/python_executors/exec_runner.py" = ["TID251"]
# Tests exercise legacy redis paths directly.
"tests/**/*.py" = ["TID251"]

[tool.ruff.lint.isort]
known-first-party = ["gigaevo"]
force-sort-within-sections = true

[tool.mypy]
python_version = "3.12"
warn_unused_configs = true
check_untyped_defs = true
warn_redundant_casts = true
strict_optional = true
no_implicit_optional = true
warn_no_return = true
exclude = ["build/", "dist/", "gigaevo/memory/_vendor/A_mem/", "gigaevo/memory/_vendor/GAM_root/"]

[[tool.mypy.overrides]]
module = [
    "loguru.*",
    "numpy.*",
    "matplotlib.*",
    "pandas.*",
    "chromadb.*",
    "sentence_transformers.*",
    "bm25s.*",
    "nltk.*",
    "transformers.*",
    "litellm.*",
    "sklearn.*",
    "tqdm.*",
    "networkx.*",
    "scipy.*",
    "wandb.*",
    "cloudpickle.*",
    "seaborn.*",
    "statsmodels.*",
    "tensorboardX.*",
    "langchain.*",
    "langchain_core.*",
    "langchain_openai.*",
    "langgraph.*",
    "langfuse.*",
    "optuna.*",
    "cma.*",
    "orjson.*",
    "diffpatch.*",
    "dotenv.*",
    "jinja2.*",
    "fakeredis.*",
    "GAM_root.*",
    "gigaevo_memory.*",
    "A_mem.*",
    "shared_memory.*",
    "runtime_config.*",
]
ignore_missing_imports = true

[tool.semantic_release]
version_variables = ["gigaevo/__init__.py:__version__"]
version_toml = ["pyproject.toml:project.version"]
commit_message = "{version}\n\nAutomatically generated by python-semantic-release"
commit_parser = "conventional"
major_on_zero = true
allow_zero_version = true
tag_format = "v{version}"

[tool.semantic_release.branches.main]
match = "(main|master)"
prerelease_token = "rc"

[tool.semantic_release.changelog]
changelog_file = "CHANGELOG.md"

[tool.semantic_release.commit_author]
env = "GIT_COMMIT_AUTHOR"
default = "semantic-release <semantic-release>"

[tool.semantic_release.commit_parser_options]
minor_tags = ["feat"]
patch_tags = ["fix", "perf"]
other_allowed_tags = [
    "build",
    "chore",
    "ci",
    "docs",
    "style",
    "refactor",
    "test",
]

[tool.semantic_release.remote]
name = "origin"
type = "github"

[tool.semantic_release.publish]
dist_glob_patterns = ["dist/*"]
upload_to_vcs_release = true

[tool.coverage.run]
source = ["gigaevo"]
branch = true
omit = [
    "tests/*",
    "gigaevo/*/__pycache__/*",
    "*/conftest.py",
]

[tool.coverage.report]
show_missing = true
skip_empty = true
fail_under = 0
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.",
    "raise NotImplementedError",
    "@abstractmethod",
    "\\.\\.\\.",
]

[tool.coverage.html]
directory = "htmlcov"
